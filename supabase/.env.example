# Used only by supabase/seed.js — the service role key bypasses Row Level
# Security, so keep this file local and NEVER commit it or expose it to the frontend.
SUPABASE_URL="https://YOUR-PROJECT-REF.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="paste-your-service-role-key-here"
